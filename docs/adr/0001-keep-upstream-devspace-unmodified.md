# Keep upstream DevSpace unmodified

Team DevSpace keeps the upstream DevSpace runtime pinned to a verified release and adds team distribution/connectivity beside it rather than maintaining a product fork. This preserves a replaceable upstream boundary, minimizes long-term self-maintained code, and makes future DevSpace upgrades an explicit Team DevSpace release decision instead of an accumulating merge burden.
