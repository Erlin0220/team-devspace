# Use a shared gateway with per-device isolation

The ChatGPT workspace publishes one shared Team DevSpace endpoint, while each Access Key is bound to exactly one enrolled Device and requests are routed only to that Device's outbound connectivity path. This avoids publishing one app per employee, avoids depending on an administrator computer, and keeps employee devices isolated even though they share one workspace app.
