name: Native packaging moved off GitHub Actions

on:
  workflow_dispatch:

permissions: {}

jobs:
  packaging-moved:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "macOS packaging now runs manually in Codemagic; Windows and Linux packaging are local-only."
